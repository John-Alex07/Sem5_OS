#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    printf("Enter the Number of Page Requests :\n");
    scanf("%d", &n);

    int page_req[n];
    for(int i = 0; i < n; i++)
        
}